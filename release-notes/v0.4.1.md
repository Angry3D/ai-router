# AI Router v0.4.1

## 重点更新

- 系统设置新增全局出站代理，可通过 HTTP、HTTPS 或 SOCKS5 代理连接外部服务。
- 推理、余额查询、模型发现和图片生成请求支持运行中切换代理，无需重启应用。
- 设置页面标题固定在内容滚动区上方，系统、Codex 和用量页面滚动时更易保持定位。

## 注意事项

- 代理仅用于外部请求，AI Router 的本地回环地址仍保持直连。
- 生成图片 URL 的下载继续使用直连和 DNS 安全校验，不经过全局代理。
- 无需手动迁移配置；安装后不会自动重启应用。
