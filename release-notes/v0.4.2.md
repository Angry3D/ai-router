# AI Router v0.4.2

## 重点更新

- 新增「生图模型」设置：可选择 Codex 生图工具使用的上游模型（默认 gpt-image-2），也可填写自定义模型名；不同模型族按各自的尺寸与质量参数校验。
- 用量页面标记上游模型转发：记录列表、请求详情与菜单用量速览在转发发生时同时显示请求模型与实际模型。

## 注意事项

- Codex 兼容性验证基线更新至 Codex CLI 0.155.1。
- 无需手动迁移配置；应用内安装更新后仍需确认重启，才能运行新版。
